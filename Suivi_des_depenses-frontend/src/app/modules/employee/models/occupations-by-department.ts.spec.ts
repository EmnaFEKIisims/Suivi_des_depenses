import { OccupationsByDepartment } from './occupations-by-departmentts';

describe('OccupationsByDepartment', () => {
  it('should create an instance', () => {
    expect(new OccupationsByDepartment()).toBeTruthy();
  });
});
